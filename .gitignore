node_modules/
.env
.env.local
.env.*.local
*.log
.DS_Store

# Local SQLite files — we do NOT commit databases
server/data.sqlite
server/*.sqlite
server/*.sqlite-journal

# Serena MCP server's local cache + project config
.serena/
